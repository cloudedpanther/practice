extends layouts/main

block content
  h1 TXT2HTML!
  
  form(action="/", method="post", enctype="multipart/form-data")
    input(type="file", name="textFile", required=true, accpet=".txt")
    input(type="submit", value="Convert!!")